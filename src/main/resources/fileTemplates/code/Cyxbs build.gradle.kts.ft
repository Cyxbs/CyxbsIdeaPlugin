plugins {
#if($BUILD_GRADLE_IS_SINGLE_MODULE)
  id("module-single")
#else
  id("module-manager")
#end
}

dependModule {
#foreach ($item in $BUILD_GRADLE_MODULES)
  $item
#end
}

dependLibrary {
#foreach ($item in $BUILD_GRADLE_LIBRARIES)
  $item
#end
}