package ${README_PACKAGE}

/**
 * 一些你需要知道的东西:
 *
 * 1. 如何删除模块?
 * 在根目录 settings.gradle.kts 中的 excludeList 集合添加模块名称，再刷新 gradle 即可取消对模块的引用，然后手动删除文件夹即可
 *
 * 2. 如何单模块调试 ?
 * - 在模块中把插件 module-manager 替换为 module-single
 * - 在 src 下创建 single 文件夹，并创建对应包名
 * - 新建一个启动类实现 ISingleModuleEntry，加上 @AutoService 注解
 * - 刷新 gradle，AS 的打包配置改为单模块的配置即可打出单模块包
 *
 *
 */
